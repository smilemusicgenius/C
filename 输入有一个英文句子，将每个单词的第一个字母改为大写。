#include<stdio.h>
#include<string.h> 
int main(){
	char a[100];
	int d,i;
	while(gets(a))
	{d=strlen(a);
	 for(i=0;i<d;i++)
		{if (i==0)
		 a[0]=a[0]-32;
		 if (a[i]==' ')
		 a[i+1]=a[i+1]-32;
		 printf("%c",a[i]);
		}
	}
}
